import { ContactPageType } from "@/types";

const ContactPageConfig: ContactPageType = {
  content: [
    {
      title: "Contact Us",
      description:
        "",
    },
  ],
};

export default ContactPageConfig;
